@*@model IEnumerable<TandooriRecipe.Models.Favourite>*@
@model TandooriRecipe.Models.Favourite
@{
    Layout = "_Layout";
}

@*<h1>List of Recipes</h1>
<table>
    <thead>
        <tr>
            <th>Favourite ID</th>
            <th>Username</th>
        </tr>
    </thead>
    <tbody>
        @foreach (TandooriRecipe.Models.Favourite r in Model)
        {
            <tr>
                <td><a asp-action="DisplayPage">@r.FavouriteID</a></td>
                <td>@r.UserName</td>
            </tr>
        }
    </tbody>
</table>*@



<form class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4" asp-controller="TandooriRecipe" asp-action="AddFavourite">
    <div class="mb-4">
        <label class="block text-gray-700 text-sm font-bold mb-2">
            Recipe Name
        </label>
        <input class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" asp-for="recipeName" type="text">
    </div>
    <div class="mb-4">
        <label class="block text-gray-700 text-sm font-bold mb-2">
            User Name
        </label>
        <input class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" asp-for="UserName" type="text">
    </div>
    <div class="mb-4 btn-container">
        <button class="btn-success btn-lg text-center btnAddFav" type="submit" asp-controller="TandooriRecipe" asp-action="AddFavourite"> Add Favourite </button>
        <button class="btn-success btn-lg text-center btnCancel" type="submit" asp-controller="TandooriRecipe" asp-action="Index"> Cancel  </button>
    </div>
</form>

    @*@if (!Model.Any()) {
            <div class="text-center m-2">
                <form asp-action="SeedDatabase" method="post">
                    <button type="submit" class="btn btn-danger">Seed Database</button>
                </form>
            </div>

        } else {

            <table class="table table-striped table-bordered table-sm">
                <tr>
                    <th class="text-right">ID</th>
                    <th>Name</th>
                    <th class="text-right">Favourite ID</th>
                    <th class="text-center">Actions</th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="text-right">@item.RecipeId</td>
                        <td>@item.recipeName</td>
                        <td class="text-right">@item.FavouriteID</td>
                        <td class="text-center">
                            <form asp-action="Delete" method="post">
                                <button type="submit" class="btn btn-danger btn-sm">
                                    Delete
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            </table>
        }*@



    @*<html>
        <body>
            @foreach (TandooriRecipe.Models.Favourite item in Model)
            {
                <h3>Ingredients: </h3>
                <div class="pie-recipe__tips">
                    <div class="pie-recipe__tips__text">
                        @item
                    </div>
                    <br>
                </div>
            }
        </body>

        </html>*@
